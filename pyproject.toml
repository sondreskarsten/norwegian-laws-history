[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "norwegian-laws-history"
version = "0.1.0"
description = "Verified observations of immutable Lovdata source evidence"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
law-history = "law_history.__main__:main"

[tool.setuptools]
packages = ["law_history"]

name: Verify independent evidence consumer

on:
  pull_request:
  push:
    branches: [main]
    paths: ['law_history/**', 'tests/**', 'pyproject.toml', '.github/**']

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install .
      - run: python -m unittest discover -s tests -v
